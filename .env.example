# Port that the server will listen on
PORT=3000

# GitHub Personal Access Token (PAT) for higher rate limits (optional but recommended)
# Generate one at https://github.com/settings/tokens
GITHUB_TOKEN=

# Gemini API Key for AI report generation
# Generate one at https://aistudio.google.com/
GEMINI_API_KEY=
